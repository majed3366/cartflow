# Merchant Visual Identity Restoration V1 — report

**Hunk classes:** SHARED_VISUAL_PRIMITIVE (selection + identity markers) · TEST · DOCUMENTATION. UNRELATED = 0.

Painter / CSS files: unchanged.

Root cause: leftover `cf_ui_v2=0` silently selected V1 on live `480d7d52`.
